name: "apollos-standard-templates"
tasks:

  javascript:
    src: "_source/**/*"
    dest: "lib/"

  markup:
    src: "_source/**/*"
    dest: "lib/"
    minify: true

  sass:
    src: "_source/**/*"
    dest: "lib/"
    comb: true
    lint: true

  "meteor-load":
    src: "lib/**/*"
    dest: "./"
    org: "newspring"
    package:
      versionsFrom: "1.1.0.2"
      export:
        "Apollos": "client"
      use:
        "blaze": "client"
        "spacebars": "client"
        "templating": "client"
        "newspring:apollos-namespace": "client"
        "newspring:apollos-components": "client"
        "newspring:apollos-helpers": "client"
