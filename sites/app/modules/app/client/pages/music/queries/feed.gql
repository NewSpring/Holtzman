query getAlbums($limit: Int!, $skip: Int!) {
  allContent(channel: "albums", limit: $limit, skip: $skip) {
    entryId: id
    title
    status
    channelName
    meta {
      urlTitle
      siteId
      date
      channelId
    }
    content {
      images {
        fileName
        fileType
        fileLabel
        s3
        cloudfront
      }
    }
    tracks {
      file
    }
  }
}
