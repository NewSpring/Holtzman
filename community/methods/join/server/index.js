/*global Meteor, check */
import { api } from "../../../../core/util/rock"
import { makeNewGuid } from "../../../../core/util/guid"
import Moment from "moment"

Meteor.methods({
  "community/actions/join": function(id, message) {

    return
  }
})
