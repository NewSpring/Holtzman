# if not Meteor.isClient
#   return
#
# MochaWeb?.testOnly ->
#
#   assert = chai.assert
#
#   describe "Junction", ->
#
#     it "should exist", ->
#       assert.isDefined junction
#
#     it "should be a function", ->
#       assert.isFunction junction
