machine:
  node:
    version: 4.2.1

dependencies:
  pre:
    - npm i -g tslint typings coveralls
    - typings i
  cache_directories:
    - "node_modules"
    - "typings"

test:
  post:
    - npm run coverage
    - coveralls < ./coverage/lcov.info
